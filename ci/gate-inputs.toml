version = 1
gate_matrix_version = 1

[runner]
label = "ubuntu-24.04"
bash_minimum = "5.2"
bash_maximum_exclusive = "6"
system_packages = ["libssl-dev", "pkg-config"]

[tools]
rust_msrv = "1.95"
rust_stable = "stable"
mdbook = "0.5.4"
python = "3.14"

[rust_components]
G01 = []
G02 = []
G03 = []
G04 = []
G05 = []
G06 = []
G07 = ["clippy"]
G07b = ["clippy"]
G08 = ["rustfmt"]
G09a = []
G09b = []

# RFC 094 R10: the lane registry. An RFC number maps to the heading of the
# section in that RFC holding its lane table. Ownership is declared by number,
# not by path, because an RFC's folder changes over its lifecycle; the checker
# resolves a number to exactly one `NNN-*.md` across rfcs/{proposed,accepted,
# done,archive}. RFC 093's heading is recorded verbatim, so RFC 093 is not
# amended by this mechanism and does not reopen. RFC 098 is the second
# source (2026-09-12): RFC 094 owns G13 and RFC 098 owns G14 and G15, each
# declared in that RFC's own table -- the mechanism R10 was built to provide.
[gate_lane_sources]
"093" = "Gate Matrix v1"
"094" = "Gate Matrix lanes owned by RFC 094"
"098" = "Gate Matrix lanes owned by RFC 098"

# RFC 094 R10: lane -> owning RFC number. Exactly one owner per lane; two
# owners for one lane is a TOML parse error rather than a condition to detect.
# [gate_matrix_exceptions] entries deliberately have no entry here: ownership
# feeds only the command-match check, from which exceptions are exempt.
[gate_owners]
G01 = "093"
G02 = "093"
G03 = "093"
G04 = "093"
G05 = "093"
G06 = "093"
G07 = "093"
G07b = "093"
G08 = "093"
G09a = "093"
G09b = "093"
G10a = "093"
G10b = "093"
G11 = "093"
G13 = "094"
G14 = "098"
G15 = "098"

# RFC 093 Gate Matrix v1 (rfcs/done/093-build-toolchain-release-gates.md),
# machine-readable expansion. scripts/ci-gate.sh executes these commands
# verbatim; scripts/check-gate-inputs.sh (A3.4) verifies this table matches
# the table in each lane's owning RFC ([gate_owners] above) exactly. Do not
# paraphrase, wrap, or reorder a command here without amending that RFC first.
[gates]
G01 = "cargo +1.95 build --workspace --all-targets --locked"
G02 = "cargo +1.95 test --workspace --locked"
G03 = "cargo +1.95 build --workspace --all-targets --all-features --locked"
G04 = "cargo +1.95 test --workspace --all-features --locked"
G05 = "cargo +stable build --workspace --all-targets --locked && cargo +stable test --workspace --locked"
G06 = "cargo +stable build --workspace --all-targets --all-features --locked && cargo +stable test --workspace --all-features --locked"
G07 = "cargo +stable clippy --workspace --all-targets --all-features --locked -- -D warnings"
G07b = "cargo +stable clippy --workspace --all-targets --locked -- -D warnings"
G08 = "cargo +stable fmt --all -- --check"
G09a = "cargo +1.95 test -p sui-id-store --features ldap --test ldap_smoke --locked -- --exact rustls_provider_and_tls_connector_reach_fixture"
G09b = "cargo +stable test -p sui-id-store --features ldap --test ldap_smoke --locked -- --exact rustls_provider_and_tls_connector_reach_fixture"
G10a = "mdbook build docs --dest-dir target/mdbook-gate"
G10b = "python3.14 scripts/check-markdown-links.py --root . README.md ROADMAP.md docs"
G11 = "python3.14 scripts/check-rfc-integrity.py --root . --policy ci/rfc-policy.toml"
G13 = "bash scripts/check-audit-matrix.sh"
G14 = "python3.14 scripts/check-markdown-links.py --root . rfcs/handoffs roadmap"
G15 = "python3.14 scripts/check-doc-authority.py --root . --policy ci/doc-authority.toml"

# RFC 093 M1b C2.1: the lane-completeness rule (scripts/check-gate-inputs.sh
# condition 7) requires every lane declared by any source RFC to be
# either a [gates] key or listed here, never both, never neither. This list
# is meant to stay this short: a lane belongs here only when it predates the
# dispatcher and already has its own working, independently reviewed entry
# point -- not as a place to defer wiring a new lane.
[gate_matrix_exceptions]
G12 = "Predates ci-gate.sh and has its own working entry point (ui-invariants-v1, RFC 093 M1a). Not migrated: churning a green gate for symmetry is not worth the risk. Currently also a dependency of M1b C5 -- a reason not to touch it right now, not a claim that this exception can never be revisited."

[actions]
checkout_v6 = "d23441a48e516b6c34aea4fa41551a30e30af803"
checkout_v4 = "11d5960a326750d5838078e36cf38b85af677262"
cache_v5 = "caa296126883cff596d87d8935842f9db880ef25"
upload_artifact_v4 = "ea165f8d65b6e75b540449e92b4886f43607fa02"
rust_toolchain_stable = "4cda84d5c5c54efe2404f9d843567869ab1699d4"
rust_toolchain_nightly = "4fd1da8b0805d2d2e936788875a7d65dbd677dc2"
setup_python_v7 = "5fda3b95a4ea91299a34e894583c3862153e4b97"
