//! A3.2 fixture crate: the source side of the audit-coverage check.
//!
//! Never compiled. `client.in_source_only` is the backward-direction desync
//! — a literal no matrix row accounts for.

pub fn emit() {
    let _ = "auth.login";
    let _ = "client.in_source_only";
}
